import { AccountTypeESPipe } from './account-type-es.pipe';

describe('AccountTypeESPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountTypeESPipe();
    expect(pipe).toBeTruthy();
  });
});
